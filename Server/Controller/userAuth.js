const handleSignUp = (req, res) => {
  res.json({ message: "Sign up is working..." });
};

const handleLogin = (req, res) => {
  res.json({ message: "Login is working..." });
};

export { handleSignUp, handleLogin };
